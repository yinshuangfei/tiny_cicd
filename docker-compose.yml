services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: tiny_cicd:latest
    environment:
      APP_NAME: tiny_cicd
      APP_VERSION: 1.0.0
      PORT: "8080"
    ports:
      - "8080:8080"
    restart: unless-stopped
